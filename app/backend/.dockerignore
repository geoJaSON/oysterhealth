# Build-context excludes for the backend image (Dockerfile.backend, context = app/backend).
# Keeps the host venv / caches out of the image (pip installs fresh from requirements.txt).
.venv
**/__pycache__
*.pyc
.pytest_cache
.ruff_cache
.mypy_cache
